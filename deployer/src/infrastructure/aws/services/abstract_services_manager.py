import logging
import boto3
from botocore.exceptions import ClientError

class abstract_reources_manager(object):
    def __init__(self)
        pass

    def create(bucket_name, region=None):

    def delete(bucket_name, region=None):
